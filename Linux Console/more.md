#programmingChallenges 

[[more]] is a command that lets you see one screen at a time of some input file. It is very limited and less convenient than [[less]], so use it instead.

## Examples
>[[ls]] -R | [[more]]

It lets us see one screen at a time of output of `ls -R`, which might be huge.